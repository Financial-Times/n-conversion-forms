import React from 'react';
import { DeliveryStartDate } from './delivery-start-date';

export default {
	title: 'Delivery Start Date',
	component: DeliveryStartDate,
};

export const Basic = (args) => <DeliveryStartDate {...args} />;
