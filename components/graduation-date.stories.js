import React from 'react';
import { GraduationDate } from './graduation-date';

export default {
	title: 'Graduation Date',
	component: GraduationDate,
};

export const Basic = (args) => <GraduationDate {...args} />;
